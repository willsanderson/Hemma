# -------- Desktop / Tablet --------
position: absolute
z-index: 3
display: grid
grid-auto-flow: column
grid-auto-columns: var(--homio-entity-col-width-desktop, 300px)
grid-column-gap: 8px
grid-row-gap: 8px
margin: 0
inset: >
  auto var(--homio-entity-right-inset-desktop,
           var(--homio-rail-left, var(--page-gutter, 8vw)))
       var(--homio-entity-bottom-desktop, 60px)
       var(--homio-rail-left, var(--page-gutter, 8vw))
padding: 20px var(--homio-entity-shadow-pad-right-desktop, 0px) 40px 0
box-sizing: border-box
scroll-snap-type: x mandatory
scroll-padding-inline: var(--homio-entity-shadow-pad-right-desktop, 0px)
overflow-x: auto
overflow-y: var(--homio-entity-overflow-y-desktop, hidden)
-webkit-overflow-scrolling: touch
grid-template-rows: var(--homio-entity-rows-desktop, repeat(1, auto))
min-height: var(--homio-entity-height-desktop, 180px)
align-content: end
justify-content: var(--homio-entity-justify-desktop, flex-start)
scrollbar-width: none
-ms-overflow-style: none

mediaquery:
  # -------- Mobile --------
  "(max-width: 768px), (max-height: 500px)":
    position: absolute
    z-index: 4
    inset: "calc(var(--entity-top-mobile, 260px) + 180px) var(--homio-rail-left, var(--page-gutter-mobile, 14px)) auto var(--homio-rail-left, var(--page-gutter-mobile, 14px))"
    margin: 0
    padding: "0 0 calc(64px + env(safe-area-inset-bottom)) 0"
    width: auto
    max-width: none
    box-sizing: border-box
    display: grid
    grid-auto-flow: row
    grid-auto-columns: none
    grid-template-columns: repeat(2, minmax(0, 1fr))
    grid-template-rows: auto
    grid-column-gap: 8px
    grid-row-gap: 8px
    justify-content: stretch
    justify-items: stretch
    align-content: start
    align-items: start
    overflow-x: visible
    overflow-y: visible
    scroll-snap-type: none
